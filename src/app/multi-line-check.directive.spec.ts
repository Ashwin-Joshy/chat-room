import { MultiLineCheckDirective } from './multi-line-check.directive';

describe('MultiLineCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new MultiLineCheckDirective();
    expect(directive).toBeTruthy();
  });
});
